# LAOS v0.9.0 Release Preparation

Date: 2026-07-06
Decision: PASS for creating the `v0.9.0` tag and GitHub Release after this release-preparation change is merged
Release target: `v0.9.0`
Platform scope: macOS with Python 3.11+
Deployment decision: not applicable for this release
Base `origin/main`: `7d0b1ffe5cc33ffeb958a4c14687454016962947`

## Purpose

This review closes the remaining repository-side preparation for the LAOS v0.9.0 source release. It does not introduce a deployment system, public service, package registry publication, desktop installer, or cloud runtime.

The release is a GitHub source release for the current local-first CLI, MCP, and Developer Bridge integration. Deployment is intentionally excluded because the repository has no defined production environment or deployable artifact target.

## Release prerequisites

The following gates have passed:

- PR #29 was squash merged into `main`.
- PR #30 aligned the post-merge status documentation and was merged into `main`.
- Stage 17 local macOS validation completed with 508 tests and zero failures.
- The latest `main` CI completed the same 508-test suite successfully.
- `compileall` completed successfully.
- Git whitespace checks completed successfully.
- Stage 17 reported zero Critical, Major, and Minor findings.

## Release scope

The v0.9.0 release includes:

- Markdown and JSONL authoritative memory storage with rebuildable SQLite FTS5 indexing;
- candidate-only memory creation and Review Gate-controlled activation;
- workspace, project, confidentiality, and restricted-content isolation;
- the unified 11-Agent JSON CLI;
- deterministic Reflection, Policy, candidate generation, and Loop Coordinator flows;
- conversation review and pluggable model backends;
- Agent Runtime, SessionStore, procedure lifecycle, Bridge event processing, and recovery;
- explicit MCP checkpoint capture and bounded read-only memory search;
- the fixed-scope Developer Bridge Adapter;
- macOS-first CI and acceptance evidence.

## Known limits

The release does not claim:

- passive or lossless browser-side ChatGPT capture;
- automatic policy approval or candidate activation;
- unattended background execution or retry;
- semantic vector search;
- multi-user authentication or owner/agent authorization;
- GUI, web frontend, desktop application, PyPI package, Homebrew formula, container image, or hosted service;
- Linux or Windows release support.

## Gate state

After this preparation change is merged:

- `release_readiness`: `ready_for_release`
- `v0.9_tag`: `ready_to_create`
- `github_release`: `pending_tag`
- `deployment`: `not_applicable`

The next authorized release actions are:

1. create annotated tag `v0.9.0` on the resulting `main` release commit;
2. publish a GitHub Release for `v0.9.0` using the scope and limitations recorded here;
3. update repository status to record the actual tag and GitHub Release identifiers.

No deployment action is required for v0.9.0.
