# LAOS v0.10.0 Release Preparation

- Date: 2026-07-11
- Scope: prepare and record the v0.10.0 source release gate
- Baseline: v0.9.0 (released 2026-07-06) + 29 commits on `main`
- Release head: the release-prep branch merge (base `751290e`, S5.4 merge
  PR #38, plus this branch's release-review fixes)

## Purpose

Record the release prerequisites, scope, and gate state for LAOS v0.10.0.
This is a local-first source release with no separate deployment target
(`deployment` remains `not_applicable`), same as v0.9.0.

## Release prerequisites

- Local full regression after the S5.4 merge and release-review fixes:
  **879 passed, 0 failed** (v0.9.0 baseline was 508; growth
  508 -> 787 -> 866 -> 879 across the four streams and this branch).
- Remote macOS CI passed on every merged PR in scope (#32, #33, #34, #36, #37, #38).
- `git diff --check` clean over the release range (a stray blank line at EOF
  in tests/test_vault_scanner.py was fixed in this release-prep branch).
- `python3 -m compileall src` passed; working tree clean.
- Frozen learning-loop surface (evidence / evaluation / enrichment /
  reflection_builder / eligibility / contract 06) verified zero-diff during
  each S5.x sliver review; contract 06 formally amended to v0.2.0.
- Phase 5 design authority: Design Baseline v4.3.1 (amendment A4 approved
  2026-07-11), implementation plans adversarially reviewed to convergence
  (S5.4: 8 rounds, final blocker count 0). Released snapshots are tracked
  in-repo under docs/design/phase-5/ for auditability; the local design
  vault remains the authoring source.

## Release scope

Four feature streams merged since v0.9.0:

1. **Atomic vault promotion (Sliver 6 line, PR #36 + follow-ups)** —
   promotion claim state, failed-attempt audit, recovery inspection and
   decision rules, orphan promotion-source cleanup, plan abandonment,
   conflicted-candidate re-review, promotion-target reselection, and the
   vault writer ADR (ADR-001 Phase 2).
2. **Phase 4 evaluation foundation (S3.x)** — evidence composition pure
   functions (S3.2), independent evaluation entry point (S3.5), controlled
   with/without-memory experiments, Phase 4 design and planning documents.
3. **Handoff capability (PR #34)** — `handoff.write` / `handoff.update`
   agent (12th registry agent), MCP write tool, and tool facade entry;
   source-backed active-project targets only, sha256 conflict detection,
   atomic writes. Workspace binding is enforced as of this release-prep
   branch (release-review finding: the workspace argument was previously
   accepted but not enforced — fixed with regression tests).
   PR #32 (recall-benchmark contract restore) and PR #33 (v0.9.0 release
   status record) are also in range as maintenance/docs items.
4. **Phase 5 learning chain (S5.1–S5.4)** — comparison-lineage hardening,
   full adapter input integrity, deterministic evaluation identity
   (utility_evaluation schema v2), copy-only enrichment snapshot, producer
   composition-total integrity (S5.1a–e); deterministic reflection builder
   with dual-anchor lineage and 40-point tamper matrix (S5.2); eligibility
   with explicit 12-row FSM matrix and four fail-closed gates (S5.3, PR #37);
   immutable review-queue request producer and the persistence layer with
   typed zero-write preflight, no-replace atomic publish, crash-replay
   convergence, and E5 closure (S5.4, PR #38).

Hard boundaries unchanged: candidate-only creation, active memory via
Review Gate only, no queue consumption or decision writing (Human Review
Phase deferred), no LLM/generative claims in the deterministic chain,
macOS-first scope, Python >= 3.11.

## Known limits

- Queue requests are produced and persisted but never consumed by LAOS
  itself; disposition decisions belong to the deferred Human Review Phase.
- Persistence path-safety guarantees assume a single writer per state_dir
  and no external directory-tree mutation (TOCTOU defense is an explicit
  non-goal, recorded in the S5.4 plan).
- Linux/Windows support remains deferred; no cross-platform claims.
- Open PR #12 (bounded distillation context) is an independent line and is
  NOT part of this release.

## Final gate state

- [x] Release-review round r1 (adversarial, external): NO-GO with 5 Major
  + 1 Minor findings — scope omissions (this record, revised), Handoff workspace
  enforcement (fixed, src/handoff.py + agents/handoff.py + tests; r2 hardened further:
  workspace is explicit-required on every write path — core keyword-only,
  facade and MCP tool without defaults — with facade-level end-to-end
  regressions),
  README agent-count/tool drift (fixed), broken sys.path bootstrap in
  tools/setup_local.py and tools/serve_laos.py (fixed), Phase-5 authority
  not repo-auditable (snapshots added). Re-review (r2) pending.
- [x] Release-review round r2 (external): NO-GO, 1 Major + 2 Minor —
  implicit workspace authority via defaults (fixed: explicit-required on
  every write path), r1 outcome record and snapshot state (fixed).
- [x] Release-review round r3 (external): NO-GO, 1 Major — the r2 fix
  accidentally made expected_sha256 required at the facade/MCP layers
  (fixed: keyword-only workspace required, expected_sha256=None optional
  restored, facade + MCP contract regressions added incl. tool-schema
  required/optional assertions).
- [x] Release-review round r4 single-point verification: **GO**
- [x] Release PR #39 merged to `main` with CI green (squash 77b3c6d)
- [x] `v0.10.0` tag created on 77b3c6d
- [x] GitHub Release published 2026-07-11 (non-prerelease, source-only)
- [x] PHASE_STATUS.json / ROADMAP updated to the new gate (this PR)

Gate approvals for this release are independent confirmations; no approval
from the v0.9.0 cycle is reused.
